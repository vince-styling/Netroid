<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <TextView
        style="@style/txv_desc"
        android:text="This sample aim to show you how Netroid resolved GridView.getView(position == 0) invoke too many times then make the grid disorder during dataset changed. It's totally benefit by NetworkImageView widget which provided by Netroid."/>

    <GridView
        android:id="@+id/grvDemonstration"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:numColumns="3" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:paddingTop="5dp"
        android:paddingBottom="10dp">

        <Button
            android:id="@+id/btnAddView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Add View"
            android:textSize="16sp"
            android:textColor="#ffffff"/>

        <Button
            android:id="@+id/btnRemoveView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Remove View"
            android:textSize="16sp"
            android:textColor="#ffffff"/>

    </LinearLayout>

</LinearLayout>