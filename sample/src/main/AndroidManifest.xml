<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.vincestyling.netroid.sample">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity
            android:name=".CommonHttpRequestActivity"
            android:label="CommonHttpRequest">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
            </intent-filter>
        </activity>

        <activity
            android:name=".ImageRequestActivity"
            android:label="ImageRequest">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
            </intent-filter>
        </activity>

        <activity
            android:name=".BatchImageRequestDiskActivity"
            android:label="BatchImageRequestDisk">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
            </intent-filter>
        </activity>

        <activity
            android:name=".BatchImageRequestMemActivity"
            android:label="BatchImageRequestMem">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
            </intent-filter>
        </activity>

        <activity
            android:name=".BatchImageRequestMultCacheActivity"
            android:label="BatchImageRequestMultCache">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
            </intent-filter>
        </activity>

        <activity
            android:name=".GridViewActivity"
            android:label="GridView"/>

        <activity
            android:name=".FileDownloadActivity"
            android:label="FileDownload"/>

        <activity
            android:name=".OffWithMainThreadPerformingActivity"
            android:label="OffWithMainThreadPerforming"/>
    </application>

</manifest>
